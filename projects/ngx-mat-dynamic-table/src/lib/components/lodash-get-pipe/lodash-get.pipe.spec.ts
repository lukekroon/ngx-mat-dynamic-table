import { LodashGetPipe } from './lodash-get.pipe';

describe('LodashGetPipe', () => {
  it('create an instance', () => {
    const pipe = new LodashGetPipe();
    expect(pipe).toBeTruthy();
  });
});
